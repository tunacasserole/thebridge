# ===========================================
# TheBridge Environment Configuration
# ===========================================

# Supabase Database (Required for production)
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/database
# Use the "Connection string" from the "Connection Pooling" section for DATABASE_URL
# Use the "Connection string" from the "Direct connection" section for DIRECT_URL
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# Anthropic Claude Integration (Required)
ANTHROPIC_API_KEY=sk-ant-...

# NextAuth Configuration (Required)
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# AUTH_URL - The canonical URL of your site (Required for non-Vercel deployments)
# Vercel sets this automatically. For other environments, set it explicitly.
# Must match the OAuth callback URLs configured in your OAuth providers.
# Examples:
#   Local development: http://localhost:3000
#   Production: https://your-domain.com
AUTH_URL=http://localhost:3000

# ===========================================
# Authentication Providers (At least one required)
# ===========================================

# GitHub OAuth
# 1. Create OAuth App at: https://github.com/settings/developers
# 2. Set "Authorization callback URL" to: {AUTH_URL}/api/auth/callback/github
#    - Local: http://localhost:3000/api/auth/callback/github
#    - Production: https://your-domain.com/api/auth/callback/github
# IMPORTANT: The callback URL in GitHub must EXACTLY match your AUTH_URL + /api/auth/callback/github
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret

# Google OAuth
# 1. Create OAuth 2.0 Client ID at: https://console.cloud.google.com/apis/credentials
# 2. Add authorized redirect URI: {AUTH_URL}/api/auth/callback/google
#    - Local: http://localhost:3000/api/auth/callback/google
#    - Production: https://your-domain.com/api/auth/callback/google
GOOGLE_ID=your-google-client-id
GOOGLE_SECRET=your-google-client-secret

# ===========================================
# Optional Integrations
# ===========================================

# Metabase Integration
METABASE_URL=https://your-metabase.example.com
METABASE_API_KEY=mb_...

# New Relic Integration
NEW_RELIC_API_KEY=NRAK-...
NEW_RELIC_ACCOUNT_ID=123456

# GitHub Integration
GITHUB_TOKEN=ghp_...
GITHUB_OWNER=your-org
GITHUB_REPOS=repo1,repo2

# Slack Integration
SLACK_BOT_TOKEN=xoxb-...
SLACK_TEAM_ID=T...

# Rootly Integration
ROOTLY_API_KEY=rootly_...

# Coralogix Integration
CORALOGIX_API_KEY=cxup_...
CORALOGIX_REGION=US1

# Jira Integration
JIRA_BASE_URL=https://your-org.atlassian.net
JIRA_EMAIL=you@example.com
JIRA_API_TOKEN=ATATT...
JIRA_PROJECT_KEY=PROJ

# Confluence Integration
ATLASSIAN_DOMAIN=your-org.atlassian.net
ATLASSIAN_EMAIL=you@example.com
ATLASSIAN_API_TOKEN=ATATT...

# Kubernetes Integration
KUBECONFIG=~/.kube/config

# Prometheus Integration
PROMETHEUS_URL=https://prometheus.example.com
